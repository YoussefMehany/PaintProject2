#include "AddUndoAction.h"

AddUndoAction::AddUndoAction(ApplicationManager* pApp):Action(pApp)
{
	undo_action_count = 0;
}
//Reads parameters required for action to execute (code depends on action type)
 void AddUndoAction::ReadActionParameters()
 {
	


 }

//Execute action (code depends on action type)
 void AddUndoAction::Execute()
 {
	
 }
 